import { Template } from 'meteor/templating';
import './podcast.html';

Template.podcast.onCreated( function() {
/*  TODO  (this - for all the templates)
  this.autorun(() => {
    new SimpleSchema({
    }).validate(Template.currentData());
  });
*/
});
